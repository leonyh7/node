extends ../layout

block content
    .container
        .row
            each item in movies
                .col-md-2
                    h3 #{item.title}
                    p: a.btn.btn-primary(href="/detail/#{item.id}", role="button") 观看预告片